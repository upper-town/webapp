# Test style

- Write tests using the Minitest framework. Use the `it`/`describe` blocks. Prefer normal test assertions over spec expectations. See examples in the codebase and follow them.
- Use methods available in custom `ApplicationRecordTestFactoryHelper` to build or create records.
