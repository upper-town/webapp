<%= render(PaginationComponent.new(@pagination)) %>

<% if @servers.empty? %>
  No results
<% else %>
  <%=
    render(Admin::TableComponent.new(
      collection: @servers,
      columns: [
        ['ID',      :id],
        ['Actions', ->(server) { render(partial: 'edit_actions_index', locals: { server: }) }],
      ],
    ))
  %>
<% end %>

<%= render(PaginationComponent.new(@pagination)) %>
