<div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
  <div>
    <h1 class="mb-1"><%= t("inside.webhook_configs.edit.title", name: @server.name) %></h1>
    <div class="d-flex flex-wrap gap-2 mt-2">
      <%= link_to inside_server_webhook_config_path(@server, @webhook_config), class: "btn btn-outline-secondary btn-sm" do %>
        <i class="bi bi-arrow-left me-1"></i>
        <%= t("inside.webhook_configs.edit.back_to") %>
      <% end %>
    </div>
  </div>
</div>

<%= render("form", form: @form, server: @server, form_url: inside_server_webhook_config_path(@server, @webhook_config), form_method: :patch, submit_label: t("inside.webhook_configs.edit.submit")) %>
