<%= render(Admin::FilterComponent.new(
  form: form,
  params_to_remove: FILTER_PARAMS,
  request: request
)) do %>
  <%= render(Admin::PeriodMultiSelectFilterComponent.new(
    form: form,
    selected_ids: selected_period_ids,
    aria_label: t("admin.server_stats.index.filter.period"),
    request: request
  )) %>
<% end %>
