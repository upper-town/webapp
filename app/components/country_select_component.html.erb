<%= form.select(
  :country_code,
  (
    options_for_select([blank_option], selected_value) +
    options_for_select(options, selected_value)
  ),
  {},
  {
    class: select_class,
    data: { "controller" => "country-select" }
  }.merge(aria_label ? { "aria-label" => aria_label } : {})
) %>
