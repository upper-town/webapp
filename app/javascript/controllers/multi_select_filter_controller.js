import BaseMultiSelectFilterController from "lib/multi_select_filter_controller"

/**
 * Multi-select for filter forms with client-side option filtering.
 * - Type to filter options (client-side)
 * - Click to toggle selection
 * - Updates hidden inputs for form submission
 * - Submits form on selection change
 *
 * Requires data-multi-select-filter-param-name-value (e.g. "game_ids").
 */
export default class extends BaseMultiSelectFilterController {
}
